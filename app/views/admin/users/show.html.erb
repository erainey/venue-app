<table>
	<tr>
		<th>Username</th>
		<th>Email</th>
		<th>Role</th>
		<th></th>
	</tr>
	<tr>
		<td><%= @user.username %></td>
		<td><%= link_to @user.email, 'mailto:' + @user.email %></td>
		<td><%= @role.name.to_s %></td>
		<td>
			<%= link_to 'Edit', edit_admin_user_path(@user) %>
		</td>
	</tr>
</table>